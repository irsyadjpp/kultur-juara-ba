rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Kunci seluruh akses untuk keamanan maksimal.
    // Landing page saat ini tidak membutuhkan koneksi database dari sisi klien.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
